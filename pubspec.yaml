name: cards_against_ai
description: A cards game server and web client.
version: 1.0.0
environment:
  sdk: '^3.3.0'

dependencies:
  rich_stdout: ^1.0.11
  web: ^1.0.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.4.12
  lints: ^6.1.0
  test: ^1.24.0
